<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 기압 센서가 있는 기기에서만 설치/동작하도록 -->
    <uses-feature
        android:name="android.hardware.sensor.barometer"
        android:required="false" />

    <!-- 알림 권한 (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 백그라운드 주차 추적 (Foreground Service) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- 자동차 블루투스 연결/해제 감지 -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- 우리집 도착 자동 감지(지오펜싱) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <!-- 재부팅 후 자동 모드 복구 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 지도 타일 다운로드 (OpenStreetMap) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ParkingFloor">

        <!-- Google Play Services 버전 (위치/활동인식 호출 안정화에 필요) -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ParkingFloor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.ParkingTrackingService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="기압 센서로 주차 층 자동 감지" />
        </service>

        <!-- 재부팅 후 자동 모드 복구 -->
        <receiver
            android:name=".geofence.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- 홈 화면 위젯 — 마지막 주차 위치 표시 -->
        <receiver
            android:name=".widget.ParkingWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/parking_widget_info" />
        </receiver>

        <!-- 빠른 주차 위젯 설정 화면 (위젯 추가 시) -->
        <activity
            android:name=".widget.QuickFloorConfigActivity"
            android:exported="false"
            android:theme="@style/Theme.ParkingFloor">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- 홈 화면 위젯 — 빠른 주차 위치 저장(층 직접 탭) -->
        <receiver
            android:name=".widget.QuickFloorWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.parkingfloor.app.QUICK_SET_FLOOR" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/quick_floor_widget_info" />
        </receiver>
    </application>

</manifest>
